name: whatspp
description: "WhatsApp status saver built with SAF access."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.8.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  docman: ^1.2.0
  gal: ^1.9.1
  google_fonts: ^6.2.1
  shared_preferences: ^2.2.3
  video_player: ^2.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
